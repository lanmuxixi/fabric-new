# 实验四运行入口

当前分支是实验四 Swarm 迁移分支：

```bash
feature/swarm-lab-exp4-replica-pow
```

业务基线是：

```bash
v0.6-pow
```

本分支只包含 Docker Swarm 部署层，不修改原实验四业务逻辑。完整执行顺序见：

```bash
deploy/swarm/LAB_EXP4_RUNBOOK.md
```
