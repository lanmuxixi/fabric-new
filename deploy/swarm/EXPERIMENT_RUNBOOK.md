# 实验三运行入口

当前分支是实验三 Swarm 迁移分支：

```bash
feature/swarm-lab-exp3-primarybz-pow
```

业务基线是：

```bash
v0.6-primarybz-pow
```

本分支只包含 Docker Swarm 部署层，不修改原实验三业务逻辑。完整执行顺序见：

```bash
deploy/swarm/LAB_EXP3_RUNBOOK.md
```
